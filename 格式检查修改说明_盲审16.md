# 格式检查修改说明（盲审意见16）

本文档对应盲审意见16要求的格式规范化结果摘要。**本次按要求未执行全文编译**，参考文献与交叉引用的一致性请以本地 `latexmk -xelatex` 清编译为准。

---

## 1. 图格式修改情况

- **间距**：在 [`main.tex`](main.tex) 导言区增加 `\AtBeginDocument`，将 `\textfloatsep`、`\floatsep`、`\intextsep` 统一设为 `0.5\baselineskip`，使图、表与正文之间的浮动体间距随正文字号与行距自动缩放，符合「约 0.5 行」要求。
- **已移除的局部覆盖**：[`Main_Spine/c3.tex`](Main_Spine/c3.tex) 中原有三处 `\begingroup` 内将上述长度设为 `8pt` 的设置已删除，避免与全文统一间距冲突（涉及 KD/BKD 对比、样本重放消融、知识巩固消融三组图表）。
- **图题位置与编号**：未改动 `\caption` 内容及模板自动生成编号逻辑；图题仍在图下方（模板默认）。若 PDF 中「图 章号-序号」格式与院系样例仍有细微差别，请在 `XJTU-thesis.cls` 或 `caption` 配置中核对。
- **仍需人工检查**：各章插图宽度（如 `0.58\textwidth` 与 `0.75\textwidth` 混用）是否需统一视觉尺寸；图中矢量字号是否达到校方「五号—七号」范围（须在读 PDF 或源矢量文件中确认）。

---

## 2. 表格式修改情况

- **间距**：与图共用上述全局浮动体长度设置；[`c4.tex`](Main_Spine/c4.tex) 等处保留的 `\begingroup` 仅用于 `\small`、`\footnotesize`、`\tabcolsep` 或 `\renewcommand{\tabularxcolumn}`，**未**再嵌套单独浮动间距。
- **表题位置与三线表**：未批量改写表格结构；正文表格均以 `booktabs` 三线表为主。若个别历史表格含竖线，需在后续通读 PDF 时逐项删除。
- **跨页表**：正文主要使用 `table` + `tabularx`，未发现强制跨页 `longtable`；若日后加入跨页表，须按意见补「表 x-x（续）」与重复表头。
- **仍需人工检查**：极宽表格是否与版心平齐；列标题单位（如准确率/\%）是否每张需要统一脚注说明。

---

## 3. 公式格式修改情况

- **与正文间距**：在 [`main.tex`](main.tex) 中将 `\abovedisplayskip`、`\belowdisplayskip`、`\abovedisplayshortskip`、`\belowdisplayshortskip` 设为 `0.5\baselineskip`，与意见「公式与正文约 0.5 行」一致。
- **「式中：」变量解释**：此前盲审15已在 [`Main_Spine/c2.tex`](Main_Spine/c2.tex)、[`c3.tex`](Main_Spine/c3.tex)、[`c4.tex`](Main_Spine/c4.tex) 为编号公式补充 `\noindent 式中：……`；本次未重复扩写，仅维持现状。
- **正文引用**：模板中 `\eqref` 输出格式可能与意见示例「式（3-2）」字面略有差异；请在 PDF 中核对后，必要时将叙述句统一为「式~\eqref{...}」或与院系一致的括号体例。
- **需作者确认**：若编译后出现公式编号与 `\tag{2-1}` 等手动标签冲突，须在 log 中排查 `multiply defined` 警告。

---

## 4. 参考文献格式修改情况

- **本次未批量改写** [`References/reference.bib`](References/reference.bib)：盲审16要求 GB/T 7714-2015 顺序编码制下的作者、标点、文献类型标志等，需在 **biber 输出 + 人工抽查** 下逐条完成；未运行编译则无法生成最终参考文献表对照。
- **建议后续步骤**：
  1. 执行一次完整编译，检查参考文献列表是否仅含正文引用条目（当前 `main.tex` 已取消 `\nocite{*}` 全库输出）。
  2. 对英文作者、期刊卷期页码、`\texttt{@online}` 的 `[EB/OL]` 等字段做逐项核对；信息缺失项勿编造，在修订清单中标注「需作者补充」。
- **正文与列表一致性**：须通过编译后 `.bcf`/`.bbl` 或 PDF 末页核对，本次未验证。

---

## 5. 其他说明

- 未改变论文研究内容、实验数据及图表/公式科学含义。
- 模板级图题格式（「图」与编号之间空格等）以 `XJTU-thesis` 类文件为准，若与学院 2021 版示例有出入，以学院最新 Word/PDF 样例为准微调。
